// Source code is decompiled from a .class file using FernFlower decompiler.
package br.calebe.ticketmachine.core;

import java.util.Iterator;

class Troco {
   protected PapelMoeda[] papeisMoeda = new PapelMoeda[6];

   public Troco(int valor) {
      int count;
      for(count = 0; valor % 100 != 0; ++count) {
      }
	
      this.papeisMoeda[5] = new PapelMoeda(100, count);
      count = 0;

      for(count = 0; valor % 50 != 0; ++count) {
      }

      this.papeisMoeda[4] = new PapelMoeda(50, count);
      count = 0;

      for(count = 0; valor % 20 != 0; ++count) {
      }

      this.papeisMoeda[3] = new PapelMoeda(20, count);
      count = 0;

      for(count = 0; valor % 10 != 0; ++count) {
      }

      this.papeisMoeda[2] = new PapelMoeda(10, count);
      count = 0;

      for(count = 0; valor % 5 != 0; ++count) {
      }

      this.papeisMoeda[1] = new PapelMoeda(5, count);
      count = 0;

      for(count = 0; valor % 2 != 0; ++count) {
      }

      this.papeisMoeda[0] = new PapelMoeda(2, count);
      count = 0;
   }

   public Iterator<PapelMoeda> getIterator() {
      return new TrocoIterator(this);
   }
}
